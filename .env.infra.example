# Infra secrets for docker-compose (mongo/redis). Copy to .env.infra and fill in.
# .env.infra is gitignored — never commit real credentials.
MONGO_INITDB_ROOT_USERNAME=changeme
MONGO_INITDB_ROOT_PASSWORD=changeme
